import { createTheme } from "@mui/material/styles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#4e4e4e",
			contrastText: "#ffffff",
		},
		secondary: {
			main: "#ff5500",
			contrastText: "#ffffff",
		},
	},
});

export default theme;
